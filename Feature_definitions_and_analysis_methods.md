# 特征定义与分析方法

本文档深入说明各特征的量纲/类型、适合的分析方式，以及建议的分析目标（对应《K-means 聚类入门》中的 5.1、5.2、5.3）。

---

## 一、5.1 再讲一层：每列「是什么」、量纲与类型

以下按**原始列**（边/事件级别）说明。

- **tweet_id / retweeter_id / parent_id**  
  - **是什么**：ID，类别变量。  
  - **量纲**：无，只是标识。  
  - **适合的分析**：分组、合并、建图（谁连到谁）；不做聚类或回归的自变量。

- **retweeted**  
  - **是什么**：通常为 0/1 或「是否被选中」的标记。  
  - **量纲**：二值。  
  - **适合的分析**：抽样权重、选择性模型；不参与聚类。

- **depth**  
  - **是什么**：这条转发在扩散树里离种子（根）的步数。  
  - **量纲**：非负整数（0, 1, 2, …）。  
  - **适合的分析**：**回归/分层**里当自变量（例如「深度对转发概率的影响」）；也可做描述（深度分布）。不适合做聚类维度（聚类是用户层面的「类型」，深度是单次事件属性）。

- **time_elapsed**  
  - **是什么**：从种子转发到当前转发的时间差（秒/分钟等）。  
  - **量纲**：连续时间。  
  - **适合的分析**：**时间分片、生存分析、动态网络**（按时间段切边）；不做聚类特征。

- **interaction_freq**  
  - **是什么**：转发者与父节点（被转发者）之间的历史互动频率（如互动次数、或归一化后的频率）。  
  - **量纲**：多为**非负连续或计数**（≥0），具体取决于你怎么算（原始计数 vs 归一化）。  
  - **适合的分析**：**聚类（用户关系强度）**、**回归（预测是否转发）**；是「关系强度」的核心指标。

- **number_exposures**  
  - **是什么**：该用户在这条扩散路径上「看到」这条推文的次数（曝光）。  
  - **量纲**：非负整数或连续（若做了平滑）。  
  - **适合的分析**：**聚类（曝光行为模式）**、**回归（曝光对转发概率的影响）**。

- **interest_similarity**  
  - **是什么**：转发者与父节点在兴趣上的相似度（如基于关注列表、话题、文本等的重叠）。  
  - **量纲**：常见为 **0～1**（或 -1～1，取决于定义）；0=完全不相似，1=完全相似。  
  - **适合的分析**：**聚类（兴趣同质性）**、**回归（同质性与转发）**；是「同质性」的典型指标。

- **structural_redundancy**  
  - **是什么**：二人社交网络的重叠程度（共同关注/共同粉丝等）。  
  - **量纲**：多为 **0～1** 或比例（重叠度）；越高表示圈子越重叠。  
  - **适合的分析**：**聚类（结构同质性）**、**回归（冗余与信息流）**。

- **reciprocity**  
  - **是什么**：二人关系是否互惠（互相关注等）。  
  - **量纲**：常为 0/1 或比例。  
  - **适合的分析**：**回归**（控制「关系对称性」）；一般不单独做聚类维度（信息量相对少）。

- **retweeting_inertia**  
  - **是什么**：用户「转发习惯」或惯性（如历史转发率、活跃度等）。  
  - **量纲**：连续或比例（0～1 或非负）。  
  - **适合的分析**：**回归**（控制个人倾向）；可做描述；聚类里通常不优先用（和「关系/曝光」维度不完全一致）。

- **friends_count_* / followers_count_* / statuses_count_* / age_***  
  - **是什么**：用户规模、活跃度、账号年龄。  
  - **量纲**：计数或天数等，**偏重、右偏**。  
  - **适合的分析**：**回归**里当控制变量或调节变量；**描述统计**；聚类可做但不一定优先（尺度、分布、与「关系类型」目标不一定直接对应）。

所以：  
- **5.1 的要点**：不是所有列都适合当「用户类型」的维度；ID、时间、深度、部分控制变量更适合做标识/回归/动态网络，而不是聚类特征。  
- **量纲**：相似度/冗余多为 0～1（或 -1～1）；频率、曝光、深度、计数等是非负连续或计数；ID、二值等是类别/二值。

---

## 二、5.2 再讲一层：为什么是这 4 个、以及它们「是什么」

聚类是在**用户层面**做的，所以用的是**每个用户在所有边上的聚合**：

- **avg_interaction**：该用户在所有相关边上与「父节点」的 interaction_freq 的**均值**。  
  - 含义：该用户与转发对象之间的**平均关系强度**。  
  - 量纲：与原 interaction_freq 一致（非负）。

- **avg_interest_sim**：interest_similarity 的**均值**。  
  - 含义：该用户与转发对象之间的**平均兴趣相似度**。  
  - 量纲：通常 0～1（或 -1～1）。

- **avg_structural_red**：structural_redundancy 的**均值**。  
  - 含义：该用户与转发对象之间的**平均结构重叠度**。  
  - 量纲：通常 0～1。

- **total_exposures**：number_exposures 的**和**（不是均值）。  
  - 含义：该用户在所有相关边上被曝光的**总次数**。  
  - 量纲：非负整数或连续（若做了平滑）。

选这 4 个的原因可以概括为：  
- 它们直接刻画「**谁在什么关系、什么曝光下转发**」：关系强度、兴趣同质、结构同质、曝光量。  
- 其他列要么是 ID/时间/深度（不是用户类型维度），要么更适合放在回归里当控制变量（如 reciprocity、inertia、粉丝数等），所以**只把这 4 个当聚类特征**，既符合研究目标，又避免把「控制变量」和「用户类型」混在一起。

---

## 三、5.3 再讲一层：不同 feature 适合怎样的分析与目标（suggestion）

按「分析类型」和「目标」来归纳，并给出简短建议。

### 1. 适合做「聚类」的（用户/关系类型）

- **目标**：把用户分成几类（如：高互动-高同质 / 低曝光-低冗余 等），便于描述和后续分层分析。  
- **建议方法**：K-means（或 GMM） on 标准化后的 **avg_interaction, avg_interest_sim, avg_structural_red, total_exposures**；用肘部法+轮廓系数选 k。  
- **注意**：total_exposures 若分布很偏，可考虑 log(1+x) 再标准化，再参与聚类。

### 2. 适合做「回归/因果/预测」的（是否转发、何时转发）

- **目标**：解释或预测「是否转发」「转发概率」等。  
- **建议方法**：GLM/GLMM（如 logistic、glmer），因变量为二值转发；自变量可包括：  
  - **核心**：interest_similarity, interaction_freq, structural_redundancy, number_exposures, depth（以及可能的交互项）；  
  - **控制**：reciprocity, retweeting_inertia, friends/followers/statuses/age（个体层面）。  
- **目标**：估计各特征的效应大小、方向，以及深度/曝光的边际效应等。

### 3. 适合做「动态/时间」分析的

- **目标**：看扩散随时间、阶段的变化。  
- **建议方法**：用 **time_elapsed** 做时间分片 → 每个时间片内算簇间边、或事件数 → 得到 dynamic_edges；画 network_time_*.png、或做简单时间序列/事件分析。  
- **目标**：描述「谁在什么阶段更活跃」「簇间关系如何随时间变化」。

### 4. 适合做「描述与分布」的

- **目标**：了解数据长什么样、是否有异常。  
- **建议方法**：对**所有**连续/计数变量做分布图（直方图、箱线图）、分簇/分组的均值表；对 depth、time_elapsed 做分布或分位数。  
- **目标**：为建模和聚类选变量、选变换（如 log）提供依据。

### 5. 仅做「标识/结构」的

- **tweet_id, retweeter_id, parent_id**：只用于合并、建图、分组，不参与聚类或回归作为「特征」。  
- **retweeted**：若为抽样标记，用于权重或说明样本代表谁。

---

## 四、简短对照表（分析目标 × 建议方法）

| 分析目标 | 建议方法 | 主要用到的 feature |
|----------|----------|--------------------|
| 用户分群（类型） | K-means + 肘部/轮廓选 k | avg_interaction, avg_interest_sim, avg_structural_red, total_exposures |
| 解释/预测「是否转发」 | GLM/GLMM（如 glmer） | interaction_freq, interest_similarity, structural_redundancy, number_exposures, depth；控制 reciprocity, inertia, 粉丝数等 |
| 扩散随时间/阶段变化 | 时间分片 + 动态边/网络图 | time_elapsed（分片），其余用于定义节点/边 |
| 了解分布、异常、尺度 | 描述统计 + 分布图 | 所有连续/计数变量 |
| 建图、定义节点与边 | 图/网络构建 | tweet_id, retweeter_id, parent_id, time_elapsed 等 |

以上把 5.1（每列是什么、量纲、用途）、5.2（为何是这 4 个、它们是什么）、5.3（为何只选这 4 个做聚类）讲深了一层，并给出了**各 feature 适合的分析方式与目标**。
