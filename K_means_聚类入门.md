# K-means 聚类入门：给初学者的解释

这份文档用通俗语言解释：什么是 K-means、为什么要选 k、肘部法和轮廓系数在做什么，以及它们在本项目里是怎么用的。

---

## 〇、为什么点击「成果图」文件名打不开？

像 `clustering_elbow.png`、`clustering_result.png` 这些图**不是一开始就有的**，而是**运行 R 脚本之后才会生成**。

- 脚本没跑过 → 项目目录下没有这些 `.png` 文件 → 点击链接时会报错「Unable to open / Unable to resolve resource」。
- **做法**：在 R 或 RStudio 里打开项目根目录，运行一次 `clustering_dynamic_network_lite.R`（或 `clustering_dynamic_network.R`），等脚本跑完，再在资源管理器里打开项目文件夹，就能看到并打开这些图。

---

## 一、聚类在做什么？（一句话）

**聚类**就是把一堆数据按「谁和谁更像」分成几组，同一组里的样本比较像，不同组之间比较不像。  
**K-means** 是其中最常用的一种方法：你先告诉电脑「我想分成 k 组」，它帮你把每个点分到某一组，并让组内尽量紧凑、组间尽量分开。

---

## 二、K-means 的直观理解

### 2.1 打个比方

想象你有很多同学，每人有两个分数：数学成绩和语文成绩。你想把大家分成几类（比如「文理均衡」「偏理」「偏文」等），但不想自己一个个贴标签，而是让算法根据分数自动分。

- 你事先说：我要分成 **k 组**（比如 k=3）。
- 算法会：
  1. 先随便选 k 个点当「组中心」；
  2. 把每个同学归到**离他最近**的那个组中心；
  3. 再根据当前每组里的人，**重新算**每组的中心（通常是这组里所有人的平均）；
  4. 重复 2、3 步，直到分组几乎不再变化。

最后每个同学都被分到某一组，同一组的人在某些特征上更接近。

### 2.2 正式一点的说法

- **输入**：很多个样本（比如每个用户），每个样本有若干**特征**（如互动频率、兴趣相似度等）。
- **你定的参数**：**k** = 想要分成几组。
- **输出**：每个样本一个**标签**（属于第 1 组、第 2 组……第 k 组）。
- **目标**：让「同一组内」的点彼此距离小，「不同组之间」距离大。K-means 通过不断移动「组中心」和重新分配样本，去逼近这个目标。

所以：**k 是你唯一要事先给定的数**；选大了或选小了，结果都会变差，所以需要「先测试再选 k」。

---

## 三、为什么不能随便选 k？（比如为什么不能总是用 4？）

- **k 太小**：很多本来不一样的人被硬塞进少数几组，组内差异大，失去「分群」的意义。
- **k 太大**：组太多，每组人很少，甚至有些组只有一两个点，容易过拟合、也不好解释。
- **k 刚好合适**：组数既能区分出不同「类型」，又不会过度细分。

不同数据集、不同研究目的，**合适的 k 不一样**，所以不能写死「k=4」，而要**用数据来选**。这就是我们在项目里加「肘部法 + 轮廓系数」的原因。

---

## 四、怎么选 k？两种常用方法

### 4.1 肘部法（Elbow Method）

**思路**：对 k = 2, 3, 4, … 各做一次 K-means，看「组内平方和」（WSS）随 k 怎么变。

- **WSS**：每个点到它所在组中心的距离的平方和；WSS 越小，说明组内越紧凑。
- 随着 **k 增大**，组变多，每个点总能找到更近的组，所以 **WSS 会一直变小**。
- 关键在**变化趋势**：一开始 k 增加，WSS 降得很快；到某个 k 之后，再增加 k，WSS 降得很慢。  
  那个「拐点」（像胳膊肘一样弯下去的地方）对应的 k，常被当作一个**候选值**。

**看图**：我们脚本会生成 `clustering_elbow.png`，横轴是 k，纵轴是 WSS。拐点附近的 k 可以重点考虑。

**局限**：拐点有时不明显，不同人可能看出不同的 k，所以通常再配合轮廓系数一起看。

---

### 4.2 轮廓系数（Silhouette）

**思路**：对每一个样本算一个「轮廓值」（-1 到 1 之间），再对所有样本取平均。**平均轮廓系数越大，说明当前 k 的聚类质量越好**。

单个样本的轮廓值大致含义：

- 它和自己这一组的点**有多近**（越近越好）；
- 它和「最近的别的组」**有多远**（越远越好）；
- 综合这两点，得到一个 -1～1 的数：越接近 1 表示「归在这一组很合理」，越接近 0 表示「可归这组也可归那组」，负值表示「可能归错组了」。

**做法**：对 k = 2, 3, 4, …, 10 各做一次 K-means，每次算平均轮廓系数，画成「k — 平均轮廓系数」的图。  
**选 k**：选**平均轮廓系数最大**的那个 k（有时也会结合业务选稍小一点的 k，避免组太多）。

**看图**：我们脚本会生成 `clustering_silhouette_choice.png`，横轴是 k，纵轴是平均轮廓系数，通常取最高点对应的 k。

---

## 五、数据里有哪些特征？为什么只用 4 个做聚类？

`data_model.Rdata` 里每一行是一条「谁转发了谁」的边，列很多，但**只有一部分适合用来做「用户分群」**。下面先列出**全部列**及其用途，再说明为什么聚类只用其中 4 个。

### 5.1 全部特征（列）列表与用途

| 列名 | 含义 | 用途说明 |
|------|------|----------|
| `tweet_id` | 推文 ID | 标识哪条推文，不参与聚类或建模。 |
| `retweeter_id` | 转发者用户 ID | 标识谁转发，用于聚合用户、建边；不直接作为聚类特征。 |
| `parent_id` | 被转发者（父节点）用户 ID | 标识转发谁，用于聚合用户、建边；不直接作为聚类特征。 |
| `retweeted` | 是否被选中等 | 抽样/筛选标记，不参与聚类。 |
| `depth` | 扩散深度 | 该转发在扩散树中的深度，多用于回归（如 main_analysis.R），不参与聚类。 |
| `time_elapsed` | 距离种子转发的时间 | 用于时间分片、动态网络；不参与 K-means。 |
| `interaction_freq` | 互动频率 | 转发者与父节点之间的历史互动频率；**聚合后 → 用于聚类**（见下）。 |
| `number_exposures` | 曝光次数 | 该用户看到这条推文的次数；**聚合后 → 用于聚类**。 |
| `interest_similarity` | 兴趣相似度 | 转发者与父节点兴趣重叠程度；**聚合后 → 用于聚类**。 |
| `structural_redundancy` | 结构冗余 | 二人社交网络重叠程度；**聚合后 → 用于聚类**。 |
| `reciprocity` | 互惠性 | 关系是否互惠，多用于回归，本项目聚类未用。 |
| `retweeting_inertia` | 转发惯性 | 用户转发习惯等，多用于回归，本项目聚类未用。 |
| `friends_count_retweeter` | 转发者关注数 | 用户规模特征，回归或描述用，本项目聚类未用。 |
| `followers_count_retweeter` | 转发者粉丝数 | 同上。 |
| `statuses_count_retweeter` | 转发者推文数 | 同上。 |
| `age_retweeter` | 转发者账号年龄 | 同上。 |
| `friends_count_parent` | 被转发者关注数 | 同上。 |
| `followers_count_parent` | 被转发者粉丝数 | 同上。 |
| `statuses_count_parent` | 被转发者推文数 | 同上。 |
| `age_parent` | 被转发者账号年龄 | 同上。 |

聚类是在**用户层面**做的：每个用户有很多条边（多条转发记录），脚本先把「边」按用户聚合成「用户级」特征，再只取其中 4 个做 K-means。

### 5.2 用于 K-means 的 4 个特征（用户级）

脚本里对每个用户做了聚合后，只用下面 4 个变量做聚类（即 `clust_vars`）：

| 聚类用特征名 | 来源列 | 聚合方式 | 含义 |
|--------------|--------|----------|------|
| `avg_interaction` | `interaction_freq` | 对该用户所有边的平均值 | 该用户与「被转发对象」的平均互动频率。 |
| `avg_interest_sim` | `interest_similarity` | 平均值 | 该用户与转发对象之间的平均兴趣相似度。 |
| `avg_structural_red` | `structural_redundancy` | 平均值 | 该用户与转发对象之间的平均结构冗余（社交圈重叠）。 |
| `total_exposures` | `number_exposures` | 对该用户所有边的**加和** | 该用户在所有相关边上被曝光次数的总和。 |

也就是说：**只有这 4 个特征被送进 K-means，其它列都不参与聚类。**

### 5.3 为什么只选这 4 个做聚类？

- **研究目的**：本项目关心的是「转发行为」与「用户关系/曝光」的关系，所以聚类时只用了与**关系强度、兴趣、网络结构、曝光**直接相关的 4 个变量，用来区分不同类型的「转发者/被转发关系」。
- **其它列**：  
  - ID、时间、深度等是**标识或结构信息**，不适合当「用户类型」的维度；  
  - 互惠、惯性、粉丝数等更多是在**回归模型**里当控制变量或解释变量，用来解释「是否转发」，而不是用来定义用户分群。  
所以当前设计是：**用 4 个关系/曝光相关特征做用户聚类**，其余特征在别处使用（回归、描述、网络构建等）。

---

## 六、在本项目里我们是怎么做的？

1. **特征**：每个用户用上面 4 个特征（互动频率、兴趣相似度、结构冗余、曝光次数）的聚合值，先标准化。
2. **试 k**：对 k = 2～10 各跑一遍 K-means，算 WSS 和平均轮廓系数。
3. **出图**：
   - `clustering_elbow.png`：肘部法，看拐点；
   - `clustering_silhouette_choice.png`：轮廓系数随 k 变化，看哪个 k 最高。
4. **自动选 k**：程序取「平均轮廓系数最大」的 k 作为聚类数（你也可以看图后手动改代码里的 k）。
5. **正式聚类**：用选定的 k 再做一次 K-means，得到每个人的簇标签，再做动态网络、画图等。

这样就不是「拍脑袋用 k=4」，而是**先测试、再选 k、再做聚类**，更适合学习和做规范分析。

---

## 七、小结（给背公式/记概念用）

| 概念 | 一句话 |
|------|--------|
| **聚类** | 按「像不像」把样本分成若干组，同组像、不同组不像。 |
| **K-means** | 你先定 k（几组），算法通过迭代「分组 → 更新组中心」把点分到 k 组。 |
| **为什么要选 k** | k 太大或太小效果都不好，合适 k 因数据而异，所以要基于数据选。 |
| **肘部法** | 画 WSS 随 k 变化的图，拐点对应的 k 作为候选。 |
| **轮廓系数** | 衡量「组内紧、组间松」的程度；平均轮廓系数最大的 k 通常较好。 |
| **本项目做法** | 先对 k=2～10 做肘部法 + 轮廓系数，自动取轮廓系数最大的 k，再用这个 k 做聚类和后续分析。 |

如果你刚学 K-means，建议先弄懂「聚类在干什么」和「k 是什么」，再看肘部法和轮廓系数的图，最后对照脚本里的 `clustering_elbow.png` 和 `clustering_silhouette_choice.png`，会更容易理解。
